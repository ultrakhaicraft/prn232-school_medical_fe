import { AppRouter } from "../app/Router"

/*Intend to be index.tsx from bulletproof-react-app */
const App = () => {
    return (
        //<AppProvider>
            <AppRouter />
        //</AppProvider>
    )
}

export default App; 